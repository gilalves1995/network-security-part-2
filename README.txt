

# No ficheiro vms-attest-proofs.sh existem 2 linhas comentadas que correm comandos docker com permissões root (pois não era possível executa-los sem a password). As linhas não comentadas docker ps e docker images são os mesmo comandos mas sem pedir permissões root. 